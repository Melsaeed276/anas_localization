---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# anas_localization Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-16

## Active Technologies
- File system — one main Markdown file in `doc/`, served via existing MkDocs build. (001-package-features-doc)
- Dart SDK >=3.3.0 <4.0.0, Flutter >=3.19.0 + Flutter SDK, intl (>=0.18.0), flutter_localizations, yaml, shared_preferences; existing code uses analyzer, http, crypto, path (002-arabic-localization-support)
- File-based (ARB/JSON/YAML/CSV); shared_preferences for app preferences; no DB (002-arabic-localization-support)
- Dart SDK >=3.3.0 <4.0.0, Flutter >=3.19.0 + Flutter SDK, intl (date/number formatting), existing lib (catalog, translation_file_parser, translation_validator, codegen_utils) (003-optional-data-type-input)
- File-based (ARB/JSON/YAML/CSV); data type stored alongside each key in the same file (structure TBD in data-model/contracts) (003-optional-data-type-input)
- File-based (ARB/JSON/YAML/CSV); data type stored alongside each key in the same file (structure in data-model/contracts) (003-optional-data-type-input)
- Dart `>=3.3.0 <4.0.0`, Flutter `>=3.19.0` + `intl`, `flutter_localizations`, `yaml`, `http`, `crypto`, `path`, `shared_preferences` (004-update-english-localization)
- File-based translation assets (`JSON`, `ARB`, `YAML`, `CSV`) plus `shared_preferences` for locale persistence (004-update-english-localization)
- Dart `>=3.3.0 <4.0.0`, Flutter `>=3.19.0` + `intl`, `flutter_localizations`, `yaml`, existing Catalog and shared libs; Flutter Material 3 (ThemeData.useMaterial3) (005-catalog-md3-ui)
- File-based localization assets (JSON/ARB/YAML/CSV); Catalog state and preferences as defined by existing catalog repositories and state stores (005-catalog-md3-ui)
- File-based localization assets (JSON/ARB/YAML/CSV); Catalog state and preferences as defined by existing catalog repositories and state stores; default language (fallbackLocale) from package initialization (005-catalog-md3-ui)
- Dart `>=3.3.0 <4.0.0`, Flutter `>=3.19.0` + Flutter Material 3 (`ThemeData.useMaterial3`), `intl`, `flutter_localizations`, `yaml`, existing Catalog and shared libs (catalog models, translation_file_parser, translation_validator, codegen_utils), `shared_preferences` for preferences (005-catalog-md3-ui)
- File-based localization assets (JSON/ARB/YAML/CSV) plus generated dictionary model; no database (005-catalog-md3-ui)

- Markdown (docs); repo uses MkDocs (see mkdocs.yml) for published site. + None for content; MkDocs + Material theme already used for doc site. (001-package-features-doc)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Markdown (docs); repo uses MkDocs (see mkdocs.yml) for published site.

## Code Style

Markdown (docs); repo uses MkDocs (see mkdocs.yml) for published site.: Follow standard conventions

## Recent Changes
- 005-catalog-md3-ui: Added Dart `>=3.3.0 <4.0.0`, Flutter `>=3.19.0` + Flutter Material 3 (`ThemeData.useMaterial3`), `intl`, `flutter_localizations`, `yaml`, existing Catalog and shared libs (catalog models, translation_file_parser, translation_validator, codegen_utils), `shared_preferences` for preferences
- 005-catalog-md3-ui: Added [if applicable, e.g., PostgreSQL, CoreData, files or N/A]
- 005-catalog-md3-ui: Added Dart `>=3.3.0 <4.0.0`, Flutter `>=3.19.0` + `intl`, `flutter_localizations`, `yaml`, existing Catalog and shared libs; Flutter Material 3 (ThemeData.useMaterial3)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
